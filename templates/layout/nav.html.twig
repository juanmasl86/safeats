<style>
    .bg-personal {
        /*background: #915d58;*/
        background: #60373c;
    }

    footer {
        position: fixed;
        left: 0;
        bottom: 0;
        width: 100%;
    }
</style>
<nav class="navbar navbar-expand-lg navbar-expand-xl navbar-dark sticky-top bg-personal border border-dark">
    <a class="navbar-brand" href="#" onclick="window.location.href='/'">
        <img src="logo4.png"/>
    </a>
    <button aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler" data-target="#navbarSupportedContent" data-toggle="collapse" type="button">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="#" onclick="window.location.href='/'">Home
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#" onclick="window.location.href='/contacto'">Contactanos</a>
            </li>
        </ul>
        <div class="form-inline my-2 pr-5">
            {% if (user is defined) and (user != 'anon.') %}
                <ul class="navicon navbar-nav mr-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"  data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <img src="user.png" width="50px" height="50px" class="mr-4">
                        </a>

                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            {% if (user.usercity is not empty) %}
                            <a class="dropdown-item profile" href="./profile" data-toggle="modal" data-target="#myModal">{{user.name}}</a>
                            <a class="dropdown-item" href="./list_allergys">Mis alergias</a>
{#                            <a class="dropdown-item" href="./enviarmensaje">Favoritos</a>#}
{#                            <a class="dropdown-item" href="./mensajes">Enviar mensajes</a>#}
{#                            <a class="dropdown-item" href="./editarPerfil">Lista de amigos</a>#}
                            {% endif %}
                            <!--ESTO SOLO APARECE SI ERES EMPRESARIO O ENCARGADO-->
                            {% if is_granted('ROLE_SUPER_USER') %}
                                <a class="dropdown-item" href="./empresas">administrar locales</a>
                            {% endif %}
                            <!--ESTO SOLO APARECE SI ERES ADMINISTRADOR-->
                            {% if is_granted('ROLE_ADMIN') %}
                                <a class="dropdown-item" href="./administracion">administracion</a>
                            {% endif %}
                            <a class="dropdown-item" href="./logout">Cerrar sesión</a>
                        </div>
                    </li>
                </ul>
            {% else %}
                <button class="btn btn-outline-warning my-2 mx-1 my-sm-0" type="button" onclick="window.location.href='/login'">Login</button>
                <button class="btn btn-outline-warning my-2 mx-1 my-sm-0" type="button" onclick="window.location.href='/register'">Registrarse</button>
            {% endif %}
        </div>
    </div>
</nav>